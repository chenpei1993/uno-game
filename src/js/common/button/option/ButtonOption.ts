export interface ButtonOption{
    width ?: number,
    height ?: number,
    text ?: string,
    font ?: string,
    backgroundColor ?: string,
    color ?: string,
    textColor ?: string,
    radius ?: number,
    img ?: HTMLImageElement,
}
